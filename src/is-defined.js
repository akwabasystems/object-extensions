/**
 * Returns true if this object is defined; otherwise, returns false
 *
 * @param {Object} object       the object to check
 * @return {Boolean} true if this object is defined; otherwise, returns false
 */
export default (object) => object !== undefined;
